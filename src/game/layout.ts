/** Раскладка в долях от min(width, height) — одинаково работает на телефоне и на десктопе. */
export const LAYOUT = {
  targetRadius: 0.15,
  spawnRadius: 0.74,
  choiceOffset: 0.24,
  ringThickness: 0.013,
  targetThickness: 0.006,
} as const;
