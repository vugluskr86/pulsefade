{
  "name": "pulsefade",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "npm run typecheck && vite build",
    "verify:gamepush": "node scripts/release/verify-gamepush-build.mjs",
    "package:gamepush": "npm run typecheck && vite build --mode gamepush && npm run verify:gamepush && node scripts/release/package-gamepush.mjs",
    "deploy": "set VITE_GAMEPUSH_ENABLED=false && npm run build && git add docs/ && git commit -m \"Deploy to GitHub Pages\" && git push",
    "release:android": "npm run package:gamepush"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0",
    "vite": "^7.0.0",
    "vitest": "^3.2.0"
  }
}
